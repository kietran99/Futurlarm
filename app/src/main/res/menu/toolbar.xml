<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search_futurlarms"
        android:title="Search"
        android:icon="@drawable/ic_search_black_24dp"
        app:showAsAction="ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView"/>

    <item
        android:id="@+id/filter_futurlarms"
        android:title="Filter"
        android:icon="@drawable/ic_filter_list_black_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/account_manager"
        android:title="@string/accountSettings"
        app:showAsAction="never"/>

    <item
        android:id="@+id/sign_out"
        android:title="@string/signOut"
        app:showAsAction="never"/>
</menu>